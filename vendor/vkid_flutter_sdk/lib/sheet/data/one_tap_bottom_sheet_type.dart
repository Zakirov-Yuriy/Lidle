part of '../../library_vkid.dart';

/// A type of bottom sheet, specifies the theme.
enum OneTapBottomSheetType {
  /// A type for bottom sheet with the light theme.
  light,

  /// A type for bottom sheet with the dark theme.
  dark,

  /// A type for bottom sheet with a theme which corresponds to the current theme in system settings.
  system,
}
