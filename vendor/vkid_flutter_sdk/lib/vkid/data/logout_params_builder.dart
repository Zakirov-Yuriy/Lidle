part of '../../library_vkid.dart';

/// A builder for [LogoutParams].
class LogoutParamsBuilder {
  /// Builds [LogoutParams] instance with values provided earlier.
  LogoutParams build() {
    return const LogoutParams._();
  }
}
